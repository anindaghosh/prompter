# Custom logo (optional) — path like /logo.png or /logo.svg; defaults to /logo.svg
# NEXT_PUBLIC_LOGO_URL=/logo.png

# ─── SpacetimeDB ─────────────────────────────────────────────────────────────
# Production (SpacetimeDB Cloud maincloud):
NEXT_PUBLIC_SPACETIMEDB_URL=wss://maincloud.spacetimedb.com
# Local dev (spacetime start):
# NEXT_PUBLIC_SPACETIMEDB_URL=ws://localhost:3000

# The database name published via: spacetime publish prompter-hack
NEXT_PUBLIC_SPACETIMEDB_MODULE=prompter-hack

# ─── SpacetimeAuth (OIDC login) ──────────────────────────────────────────────
# Built-in SpacetimeDB OIDC provider. Create a project/client in the SpacetimeAuth
# dashboard, then register redirect URIs (e.g. http://localhost:3001/callback and
# your production <origin>/callback). Public/SPA client — no client secret in the app.
NEXT_PUBLIC_SPACETIMEAUTH_AUTHORITY=https://auth.spacetimedb.com/oidc
NEXT_PUBLIC_SPACETIMEAUTH_CLIENT_ID=

# ─── Gemini API ──────────────────────────────────────────────────────────────
# Used by /api/generate-image and /api/score-image
# Get key from https://ai.google.dev/
GEMINI_API_KEY=
